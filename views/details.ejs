<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/main.css">
    <title>Detail Page</title>
</head>
<body>
    <section class="details">
    <h1 class="details-header">DETAILS</h2>
    <% if (result) { %>
        <h2 class="animal-detail"><%= result.name.charAt(0).toUpperCase() + result.name.slice(1) %> Details</h2>
        <li>Family:   <%= result.type.charAt(0).toUpperCase() + result.type.slice(1) %></li>
        <li>Breed:   <%= result.breed.charAt(0).toUpperCase() + result.breed.slice(1) %></li>
        <li>Color:   <%= result.color.charAt(0).toUpperCase() + result.color.slice(1) %></li>
        <li>Age:  <%= result.age %></li>
    <% } else {
        document.location.href="/";
    } %>
    </section>
    <div class="home-button"><a href="/">Back to home</a></div>
</body>
</html>